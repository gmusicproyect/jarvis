# Evidencia de operador (RC 1.0.0)

Archivos que estaban en el Escritorio durante la validación Mac limpio.
El código vive en este repo; el DMG en `dist/` (local, no versionado).

| Ruta | Contenido |
|------|-----------|
| `captures/` | Salidas de `clean_mac_capture.sh` |
| `audits/` | Bundles de auditoría previos |
| `jarvis_rc1_dmg.sha256` | Huella del DMG anclado |
| `JARVIS_EVAL_BRIEF_PARA_CLAUDE.md` | Brief de evaluación pre-DMG |
| `release_validate_741dd82.log` | Log histórico |

Binarios grandes (backup, eval tar.gz, PNG): `../artifacts/desktop/` (gitignored).
